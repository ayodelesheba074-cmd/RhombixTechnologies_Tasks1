network sniffer tool
this script captures network packets on the en0 interface
it identifies source and destination ip addresses
it displays protocols as tcp udp or icmp
it records timestamps for every packet
it saves all data to a file called sniffer_log.txt
install scapy using pip install scapy
run the script using sudo python3 sniffer.py
